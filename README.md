# chap7.0

## Documentation

Topic-based documentation is available in the `documentation/` folder.

## Deployment

- On every `push` to the repository, deployment starts through **GitHub Pages**.
- After deployment, the published JS script is connected in **Webflow** via a `<script>` tag.
